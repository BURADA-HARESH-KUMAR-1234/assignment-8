
//Armstrong program
#include <stdio.h>

void main()
{
  int n,r,sum=0,temp;
  scanf("%d",&n);
  temp=n;
  while(n>0)
  {
    r=n%10;
    sum+=(r*r*r);
    n/=10;
  }
  if(temp==sum)
  printf("%d is a Armstrong number",temp);
  else
  printf("%d is not a Armstrong number",temp);
}
  


